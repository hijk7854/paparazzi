import React from 'react';

import styles from './TreePane.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);


const TreePane = () => (
  <div>
    TreePane
  </div>
);


export default TreePane;